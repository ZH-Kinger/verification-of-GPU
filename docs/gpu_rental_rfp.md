# GPU 算力租赁需求书（RFP）

版本 v1.0 ｜ 编制日期 2026-08-12 ｜ 首批期望交付 **2026-10-15**

本文档用于向算力供应商征询方案与报价。请**逐条应答**，不要用"支持/满足"一类的
词回复——凡是标了 `【应答】` 的地方都要填**具体数值、具体型号、具体做法**。
无法满足的条目请直接写"不满足"并说明替代方案，我方不因坦白扣分，但会因应答与
实测不符而拒收。

横向比较用的应答表见 `templates/vendor_response_template.csv`，请一并填回。

---

## 0. 需要甲方（我方）内部先确认的事项

以下几项在发出本文档前必须敲定，文中已按假设值填写并加了标记：

| 编号 | 待确认项 | 文中暂按 | 影响 |
|---|---|---|---|
| C1 | 首批到底是 B300 还是 H200 | 两套都要报价，B300 优先 | 整机、网络、电力、机柜方案完全不同，报价差约一倍 |
| C2 | 扩容批次的期望交期 | 【待填】 | 不影响首批，但影响 fabric 与机柜的预留量 |
| C3 | 长稳烤机是 18h 还是 24h | 18h（64800s，见附录 A） | 验收周期与租金起算点 |
| C4 | 单卡预算上限 / 总预算 | 未填 | 决定是否接受 H20 等替代型号 |

---

## 1. 采购概述

### 1.1 用途

分布式模型训练，模型类型为 **VLA（Vision-Language-Action）** 与 **world model**。

这两类负载对平台的要求与常规 LLM 训练不同，供应商在设计方案时必须考虑：

- **数据以视频 / 图像帧为主**，数据集体量大、单文件小、随机读多。存储的**元数据性能**
  和**聚合读带宽**是瓶颈，不是容量。
- **单步计算相对轻、数据吞吐相对重**，dataloader 打不满会直接拉低 GPU 利用率。
- **长序列 / 大 batch**，显存容量敏感，这是我方倾向 B300（288GB）而非 H100（80GB）的原因。
- **训练周期长**，单次训练动辄数周，因此**有效训练时间占比（Goodput）比峰值算力更重要**。

### 1.2 规模与交期

| 批次 | 规格 | 节点数 | GPU 数 | 期望交付 |
|---|---|---|---|---|
| 首批 | B300 8 卡节点（备选 H200） | 16 | 128 | **2026-10-15** |
| 扩容批 | 同首批型号，同 fabric 域 | +16 | +128（累计 256） | 【待填，见 C2】 |
| 长期规划 | 同型号 | 递增至百卡～千卡级 | — | 按需预约 |

**扩容是硬需求，不是选项。** 供应商方案中必须说明：首批交付时**为扩容预留的
fabric 端口、机柜位、供电容量**分别是多少，以及在不停机、不迁移、不降低跨节点
通信性能的前提下，最大可就地扩容到多少卡。

> ⚠️ **扩容必须在同一个无收敛 fabric 域内。** 如果扩容节点被放到另一个 fabric 域、
> 或与首批节点之间存在收敛比，则 128→256 卡的跨节点 AllReduce 性能会显著劣化，
> 这种扩容对我方无价值。请在应答中明确承诺同域扩容的上限卡数。

---

## 2. 需求一：算力集群

### 2.1 GPU 型号与整机配置

可接受型号：**B300 / B200 / H200 / H100 / H20**，优先级 B300 > B200 > H200 > H100 > H20。

各型号的**最低整机配置要求**如下。表中"计算网卡"一列是本次采购的重点，
不同卡型要求不同，**不得跨型号套用**：

| GPU 型号 | 形态（必须） | 单卡显存 | NVLink 域 | 单卡 NVLink 双向 | **计算网卡（必须）** | 整机计算网聚合 | 单卡 TDP | 整机功耗（估） |
|---|---|---|---|---|---|---|---|---|
| **B300** | HGX 8-GPU SXM + 2× NVSwitch4 | 288 GB HBM3e | 8 卡单域 | 1.8 TB/s | **8× ConnectX-8 800Gb/s，1:1** | **6.4 Tb/s** | 1100 W | ≈13 kW |
| **B200** | HGX 8-GPU SXM + 2× NVSwitch4 | 180 GB HBM3e | 8 卡单域 | 1.8 TB/s | **8× CX-8 800G（首选）或 8× CX-7 400G（最低）** | 6.4 / 3.2 Tb/s | 1000 W | ≈12 kW |
| **H200** | HGX 8-GPU SXM + 4× NVSwitch3 | 141 GB HBM3e | 8 卡单域 | 900 GB/s | **8× ConnectX-7 400Gb/s，1:1** | **3.2 Tb/s** | 700 W | ≈10 kW |
| **H100** | HGX 8-GPU SXM + 4× NVSwitch3 | 80 GB HBM3 | 8 卡单域 | 900 GB/s | **8× ConnectX-7 400Gb/s，1:1** | **3.2 Tb/s** | 700 W | ≈10 kW |
| **H20** | HGX 8-GPU SXM | 96 或 141 GB | 8 卡单域 | 900 GB/s | **8× CX-7 400G（首选）／8× 200G（可接受，须注明）** | 3.2 / 1.6 Tb/s | ≈400 W | ≈7 kW |

`【应答】` 请按实际供货型号填写：GPU 型号与显存版本、整机品牌与 SKU、
NVSwitch 数量与版本、网卡型号与口速、单节点满载实测功耗。

其余整机配置（各型号通用最低要求）：

| 部件 | 最低要求 | 说明 |
|---|---|---|
| CPU | 2 路，单路 ≥ 48 核 | dataloader 与视频解码是 CPU 密集的，核数不足会成为 VLA 训练的瓶颈 |
| 系统内存 | B300/B200 ≥ 3 TB；H 系列 ≥ 2 TB | 需 ≥ GPU 显存总量的 1.5 倍，用于数据预取与 checkpoint 暂存 |
| 内存配置 | 所有 DIMM 同容量、同频率、全通道插满 | 缺条或混插会造成节点间性能离散 |
| 本地 NVMe | ≥ 4 × 3.84 TB，可组 RAID0 做 scratch | 用于数据集本地缓存与 checkpoint 落盘缓冲 |
| 系统盘 | 2 × ≥960 GB，RAID1 | |
| 存储网卡 | 独立于计算网卡，≥ 2 × 100 GbE | **不得与计算网复用**，见 2.3 |
| 带外管理 | BMC / IPMI，独立网口 | 需开放给我方，用于远程重启与硬件告警 |

### 2.2 NVLink：8 卡单域（硬性否决项）

**每个节点的 8 颗 GPU 必须处于同一个 NVLink 域内，通过 NVSwitch 全互联。**

验收判据（我方逐台实测，见附录 A §4）：

- `nvidia-smi topo -m` 中 **56 个 GPU 对（8×7）全部显示 `NV18`**，不允许出现
  `PIX` / `PXB` / `PHB` / `SYS` / `NODE`。
- 每卡 NVLink 链路数 = **18 条**，全部 `Active`。
- `nvidia-smi nvlink -e` 的 CRC / Replay 错误计数 = 0。
- P2P 单向带宽：B 系列 ≥ 750 GB/s，H 系列 ≥ 345 GB/s；P2P 延迟 ≤ 5 μs。
- 8 卡 NCCL AllReduce Bus BW：B 系列 ≥ 800 GB/s，H 系列 ≥ 350 GB/s。
- Fabric Manager 服务处于 `active (running)` 状态，NVSwitch 全部在位无降级。

**明确拒收以下形态**，出现任意一种该节点判定不通过、不起租：

1. **PCIe 版 GPU + NVLink Bridge** —— 只能两两互联，8 卡张量并行会退化到走 PCIe。
2. **4+4 拆域**或任何非 8 卡单域的 NVLink 分区。
3. **NVSwitch 故障降级**运行（部分链路不通但系统仍可启动）。
4. **Fabric Manager 未启动**或版本与驱动不匹配，导致 NVLink 未建链。
5. **MIG 已开启**或 GPU 被虚拟化切分（vGPU / 直通给多租户）。我方要求整机独占。

> 为什么单列这一条：以上几种情况在跑单卡推理或 `nvidia-smi` 看卡数时**完全看不出来**，
> 只有在 8 卡张量并行 / FSDP 时才暴露，那时候机器已经起租了。

`【应答】` 请提供一台样机的 `nvidia-smi topo -m` 完整输出、`nvidia-smi nvlink -s` 输出，
以及 8 卡 `all_reduce_perf` 的实测结果截图或日志。

### 2.3 集群网络

#### 2.3.1 组网架构

| 项目 | 要求 |
|---|---|
| 计算网络 | InfiniBand 或 RoCE v2 均可，**须明确说明是哪一种**及交换机型号 |
| 配比 | **GPU : 计算网卡 = 1 : 1**，不接受 2:1、4:1 |
| 收敛比 | **1:1 无收敛**（full fat-tree / rail-optimized） |
| 拓扑 | 轨道优化（rail-optimized）：节点内第 i 张网卡接入第 i 组 leaf |
| PCIe | 每张计算网卡独占 PCIe Gen5 x16（400G）/ **Gen6 x16（800G）** |
| GPU-NIC 亲和 | `nvidia-smi topo -m` 中 GPU 与其配对网卡为 `PIX` 或 `PXB`，**不得为 `SYS`** |
| GPUDirect RDMA | 必须可用（`nvidia_peermem` 已加载或 dmabuf 路径打通），ACS 关闭 |
| 存储网络 | **独立物理网络**，≥ 2 × 100 GbE |
| 管理网络 | 独立，含带外 BMC |

> ⚠️ **两个常见的隐性缩水，请在应答中正面回应：**
> 1. **800G 网卡插在 PCIe Gen5 槽上。** Gen5 x16 单向约 504 Gb/s，CX-8 的 800G 口
>    在 Gen5 槽上永远跑不到线速。B300 平台必须是 Gen6 x16。
> 2. **GPU 与其网卡跨 CPU socket（topo 显示 `SYS`）。** GDR 流量要绕 UPI，
>    实测带宽和延迟都会明显劣化，而 `ibstat` 看一切正常。

#### 2.3.2 网络性能指标（必须应答实测值）

延迟按跳数分档，**不接受笼统的"≤10μs"**。测试工具 `ib_write_lat`，消息 2 Byte，
取 P99：

| 场景 | 要求 | `【应答】` 实测 |
|---|---|---|
| 同 leaf 交换机下（1 跳） | ≤ 2.5 μs | |
| 跨 leaf、经 spine（3 跳） | ≤ 4 μs | |
| 集群内任意两节点最坏值 | ≤ 10 μs | |

带宽，测试工具 `ib_write_bw -s 65536 -x 3 -q 4 --report_gbits`：

| 场景 | B300（800G 口） | H200/H100（400G 口） | `【应答】` 实测 |
|---|---|---|---|
| 单口 write 带宽 | ≥ 720 Gb/s | ≥ 360 Gb/s | |
| 单口 read 带宽 | ≥ 720 Gb/s | ≥ 360 Gb/s | |
| GPUDirect RDMA 带宽 | ≥ 700 Gb/s | ≥ 350 Gb/s | |
| 整机 8 口聚合 | ≥ 5.7 Tb/s | ≥ 2.8 Tb/s | |

RoCE 方案额外要求（IB 方案免填）：MTU 9000、trust mode `dscp`、
PFC pause 帧占比 < 0.1%、RX discards = 0。

#### 2.3.3 跨节点 NCCL（集群级验收的核心指标）

`all_reduce_perf -b 512M -e 8G -f 2 -g 1`，每节点 8 GPU，Bus BW 最低值：

| 节点数 | B300 要求 | H200 要求 | `【应答】` 实测 |
|---|---|---|---|
| 2 节点 / 16 卡 | ≥ 720 GB/s | ≥ 300 GB/s | |
| 4 节点 / 32 卡 | ≥ 700 GB/s | ≥ 290 GB/s | |
| 8 节点 / 64 卡 | ≥ 680 GB/s | ≥ 280 GB/s | |
| 16 节点 / 128 卡 | ≥ 660 GB/s | ≥ 270 GB/s | |

**规模衰减是关键。** 从 2 节点到 16 节点，Bus BW 下降不得超过 10%。
下降过大说明存在收敛、拓扑不优或路由哈希冲突。

`【应答】` 请提供 16 节点（全量首批规模）的 AllReduce / AllGather /
ReduceScatter / AllToAll 实测日志。**这是我方集群级验收的必测项，
建议贵司在交付前自测，避免交付后返工。**

### 2.4 电力、制冷与机房

| 项目 | `【应答】` |
|---|---|
| 单机柜供电上限（kW） | |
| 单机柜可放置的 GPU 节点数 | |
| 冷却方式（风冷 / 冷板液冷 / 浸没） | |
| B300 风冷是否可行，还是必须液冷 | |
| 首批 16 节点需要几个机柜、总配电容量 | |
| 扩容到 32 节点是否需增加机柜/配电，是否已预留 | |
| 供电冗余（2N / N+1）、UPS 后备时长、是否有柴发 | |
| 机房 PUE、所在城市与具体园区 | |
| 是否具备 A 级机房 / 等保 / 相关认证 | |

> 参考：16 节点 B300 按 13 kW/节点估算约 208 kW（不含网络与制冷）；
> H200 按 10 kW/节点约 160 kW。请以贵司实测为准。

### 2.5 软件栈与框架兼容

| 项目 | 要求 | `【应答】` |
|---|---|---|
| 操作系统 | Ubuntu 22.04 / 24.04，**给 root** | |
| GPU 驱动 | B 系列 ≥ 580.105；H 系列 ≥ 550.54；**全集群版本一致** | |
| CUDA | B300 ≥ 13.0；H 系列 ≥ 12.4 | |
| NCCL | ≥ 2.30 | |
| 是否允许我方自行升级驱动/CUDA/内核 | 需允许 | |
| 深度学习框架 | PyTorch（主）、TensorFlow、JAX 可选 | |
| 并行框架 | **Megatron-LM、DeepSpeed、FSDP / FSDP2、TorchTitan** 需实测可跑 | |
| 混合精度 | BF16、FP8（B 系列须支持 FP8 训练） | |
| 通信库调优 | 是否提供针对本集群拓扑的 NCCL 环境变量推荐配置 | |

> 说明：原需求只提到 DeepSpeed，实际 VLA / world model 训练更多用
> **Megatron-LM 的 TP/PP/CP 组合**与 **FSDP2**，请一并确认可用性。
> 特别是 context parallel（长序列必需）在贵司网络上的实测效率。

`【应答】` 请提供一份**开箱可跑的环境清单**：驱动、CUDA、cuDNN、NCCL、
PyTorch、NCCL 调优参数的具体版本号与取值。

### 2.6 稳定性与故障恢复

原需求"提供故障自动恢复机制"过于笼统，无法验收。改为以下可量化条款：

| 指标 | 要求 | `【应答】` |
|---|---|---|
| **有效训练时间占比（Goodput）** | 月度 ≥ 95% | |
| Goodput 定义 | (总运行时长 − 因平台故障导致的中断时长 − 回退到上次 checkpoint 损失的时长) / 总运行时长 | |
| 集群可用率 | 月度 ≥ 99.5% | |
| 单次训练最长无故障连续运行时长（128 卡） | ≥ 72 小时 | |
| 故障检测到自动恢复（RTO） | ≤ 15 分钟 | |
| **热备节点比例** | ≥ 3%，且首批 16 节点至少配 1 台热备 | |
| 坏卡更换时限 | 现场备件 ≤ 4 小时；需返修 ≤ 24 小时 | |
| 月度掉卡率 | ≤ 0.5%（按卡·月计） | |

自动恢复机制需覆盖以下故障类型，请逐项说明检测手段与处置动作：

1. **GPU XID 错误 / ECC 不可纠错** —— 自动隔离该卡与该节点
2. **NCCL 通信超时 / hang** —— 自动识别是哪个 rank 掉队，而不是简单重启全部
3. **IB / RoCE 链路 down、PFC 风暴**
4. **NVLink CRC 错误累积**
5. **节点宕机 / 掉电**
6. **静默数据损坏（SDC）** —— 计算结果错误但无任何报错。请说明是否有定期
   DCGM diag -r 3/4 巡检或数值校验机制。**这是千卡集群最难查的一类故障。**

恢复流程需说明：是否自动从最近 checkpoint 拉起、是否自动替换为热备节点、
是否需要人工介入、通知渠道是什么。

`【应答】` 请提供贵司同规模集群近 3 个月的**真实故障统计**：故障次数、
类型分布、平均恢复时长、Goodput 实际值。

### 2.7 集群管理平台

| 功能 | 要求 | `【应答】` |
|---|---|---|
| 监控指标 | GPU 利用率、显存占用、功耗、温度、**降频原因（clock throttle reason）**、ECC 计数、**NVLink 错误**、**IB/RoCE 端口错误与 PFC 计数**、CPU、内存、本地盘、网络带宽 | |
| 指标采集粒度 | ≤ 15 秒 | |
| 历史数据留存 | ≥ 90 天，可导出 | |
| 告警 | 支持自定义阈值，推送到我方（邮件/钉钉/企微/Webhook） | |
| 任务管理 | 提交、排队、优先级、抢占、超时终止 | |
| 资源调度 | **拓扑感知调度**（同 leaf 优先分配）、整节点分配、gang scheduling | |
| 多租户 | 项目级配额、按用户/项目的用量与费用统计 | |
| 日志 | 训练日志实时查看与下载，保留 ≥ 30 天 | |
| 远程登录 | SSH（跳板机或 VPN），说明网络路径与是否限速 | |

> `降频原因` 一列不是可选项：GPU 因供电不足或散热不良而降频时，利用率仍然是 100%，
> 只有 `clocks_throttle_reasons` 能看出来。缺这个指标等于看不见机房问题。

### 2.8 开发环境与镜像

| 项目 | 要求 | `【应答】` |
|---|---|---|
| 开发机 | 预置 Ubuntu 环境，可申请 1～8 卡规格 | |
| 开发机与训练集群 | **软件栈必须同构**（同驱动、同 CUDA、同 NCCL），避免"开发机能跑集群跑不了" | |
| 开发机 → 镜像 | 支持将开发机环境打包为 Docker 镜像 | |
| 镜像 → 开发机 | 支持从 Docker 镜像创建开发机 | |
| 私有镜像仓库 | 提供，容量 ≥ 2 TB，支持我方推送自建镜像 | |
| 镜像加速 | 是否有 NGC / Docker Hub / HuggingFace 的内网加速或镜像源 | |
| 容器运行时 | NVIDIA Container Toolkit，容器内需能用 RDMA 与 GPUDirect | |
| 开发机是否挂载同一份存储 | 必须，与训练集群共享数据集与 checkpoint 目录 | |

---

## 3. 需求二：数据存储与传输

### 3.1 容量与分层

| 层级 | 容量 | 性质 | `【应答】` 报价与产品名 |
|---|---|---|---|
| 热存储（训练直读） | 首批 ≥ 300 TB，可弹性扩至 1 PB | 高性能并行文件系统（PFS） | |
| 冷存储 | ≥ 2 PB，可扩 | 标准对象存储 | |
| 本地 scratch | 每节点 ≥ 15 TB NVMe | 见 2.1 | |

要求：

- 热/冷之间支持**生命周期策略自动分层**，并可手动指定。
- 弹性扩容**不中断业务**，说明扩容粒度与生效时长。
- 说明容量是按用量计费还是按预置容量计费。

### 3.2 存储性能（必须应答实测值）

VLA / world model 的数据是视频与图像帧，**小文件元数据性能和聚合读带宽比容量更重要**。

| 指标 | 128 卡要求 | 256 卡要求 | 推导依据 | `【应答】` 实测 |
|---|---|---|---|---|
| 热存储聚合顺序读带宽 | ≥ 200 GB/s | ≥ 400 GB/s | 每 GPU dataloader 稳态约 1 GB/s，留 1.5× 余量 | |
| 热存储聚合写带宽 | ≥ 100 GB/s | ≥ 200 GB/s | 主要用于 checkpoint | |
| 单客户端（单节点）读带宽 | ≥ 20 GB/s | 同 | 8 卡 × 1 GB/s + 余量 | |
| 小文件随机读 IOPS（4 KB） | ≥ 2,000,000 | ≥ 4,000,000 | 视频帧级随机采样 | |
| 元数据 ops/s（stat / open） | ≥ 500,000 | ≥ 1,000,000 | 大目录遍历与 shuffle | |
| 单目录可容纳文件数 | ≥ 1,000,000 | 同 | 未打包的原始帧数据集 | |
| checkpoint 写入耗时 | 2 TB 全量 ≤ 5 分钟 | 同 | 折算需 ≥ 7 GB/s 持续写 | |

`【应答】` 请说明测试方法（fio / mdtest / IOR 参数），并提供原始日志。
**不接受产品彩页上的标称值。**

其他：

| 项目 | 要求 | `【应答】` |
|---|---|---|
| 访问协议 | POSIX（必须）、NFS、S3 兼容接口 | |
| 并发挂载客户端数 | ≥ 300 | |
| 数据可靠性 | 多副本或 EC，说明具体策略与设计年可靠性 | |
| 快照 | 支持，说明快照数量上限与开销 | |
| 备份 | 是否提供、如何计费 | |

### 3.3 数据迁移（火山 TOS / 阿里 OSS → 贵司）

| 项目 | 要求 | `【应答】` |
|---|---|---|
| 待迁移数据量 | 数十 TB（首批），后续持续增量 | |
| 迁移工具 | 是否提供托管迁移服务或工具 | |
| 迁移带宽 | 实测 TOS→贵司、OSS→贵司 的稳定速率（MB/s） | |
| 迁移方式 | 公网 / 专线 / 云间高速，分别的速率与费用 | |
| 完整性校验 | 必须逐对象 checksum 校验并出报告 | |
| 增量同步 | 支持断点续传与增量同步（迁移期间源端仍在写） | |
| 迁移期间双读 | 是否支持（迁移未完成时可从源端回源读） | |
| 费用 | 迁移流量、请求数、源端出流量分别谁承担 | |
| 预估耗时 | 按 50 TB 估算全量迁移完成时间 | |

> 注意：**源端（火山/阿里）的公网出流量费通常由我方承担且价格不低**，
> 请在方案中明确这笔费用的估算，以及是否有云间专线可以规避。

### 3.4 数据下载加速（国内外，数百 TB）

| 项目 | 要求 | `【应答】` |
|---|---|---|
| 国内公开数据源下载速率 | 实测 MB/s | |
| 境外数据源（如 HuggingFace、学术站点）下载速率 | 实测 MB/s | |
| 加速方案 | 具体是什么（专线 / 加速节点 / 缓存代理），是否合规 | |
| 出口带宽 | 独享还是共享，峰值与保底 | |
| 是否有 HuggingFace / 常用数据集的内网镜像 | | |
| 下载流量计费 | 单价与是否有免费额度 | |
| 并发下载任务数上限 | | |

> **合规提示：** 涉及跨境数据传输的方案，请说明合规依据。我方不接受
> 任何合规性存疑的加速手段。

### 3.5 数据预热

| 项目 | 要求 | `【应答】` |
|---|---|---|
| 预热能力 | 将指定路径（数据集、模型权重）从冷存储/对象存储提前加载至热缓存 | |
| 手动触发 | 支持，可指定路径/前缀/文件列表 | |
| 定时预热 | 支持 cron 式定时配置 | |
| 预热进度可见 | 可查询已预热比例与预计完成时间 | |
| 缓存容量与淘汰策略 | 说明缓存层容量、淘汰算法、能否 pin 住不淘汰 | |
| 预热速率 | 实测 GB/s | |
| 预热后读延迟 | 相比未预热的改善倍数 | |
| API/CLI 可调用 | 必须，见第 4 节 | |

### 3.6 第三方存储接入

| 问题 | `【应答】` |
|---|---|
| 能否直接挂载/接入第三方对象存储（火山 TOS、阿里 OSS、腾讯 COS、AWS S3）作为数据源 | |
| 接入方式（透明缓存网关 / 定期同步 / 直读） | |
| 直读第三方存储时的带宽与延迟 | |
| 能否接入第三方高速并行文件存储 | |
| 跨云流量费用由谁承担 | |

---

## 4. 需求三：API / SDK / CLI

要求提供可编程接口，覆盖以下操作。**我方会把这些接口接入自有的训练调度流水线，
纯 Web 控制台不满足需求。**

| 能力域 | 需要支持的操作 | `【应答】` 是否支持 / 接口形式 |
|---|---|---|
| 计算资源 | 查询规格与库存、创建/释放实例、开关机、重启、查询状态 | |
| 任务 | 提交训练任务、查询状态、终止、获取日志、设置优先级 | |
| 存储 | 创建/挂载/卸载卷、上传下载、**触发数据预热**、查询预热进度 | |
| 镜像 | 推送/拉取/列出私有镜像、从镜像创建开发机 | |
| 监控 | 拉取 GPU/网络/存储指标（**建议兼容 Prometheus / OpenMetrics**） | |
| 告警 | 配置告警规则、Webhook 回调 | |
| 账单 | 按项目/用户/时间段查询用量与费用明细 | |
| 权限 | 子账号、API Key、RBAC | |

| 项目 | `【应答】` |
|---|---|
| CLI 工具名称与文档地址 | |
| SDK 语言（Python 必须） | |
| 是否有 OpenAPI / Swagger 规范文档 | |
| 是否支持 Terraform / Ansible | |
| API 限流策略 | |
| 是否提供沙箱环境供我方先行联调 | |

---

## 5. 交付与验收

### 5.1 验收原则

> **未通过验收的机器不起租、不计费。** 这是本次采购的前置条件，请在应答中确认接受。

我方已建成**离线自动化验收工具链**（可脱网、可在无操作系统的裸机上从 U 盘启动运行），
逐台执行并输出带具体数值的判定表。验收不依赖贵司提供的监控数据。

判定规则：

- 任意一项 **FAIL** → 该节点不通过。
- 无 FAIL 但存在 **SKIP**（某项无法测得）→ 该节点判 **HOLD**，需说明后重测。
  **SKIP 不等于通过。**
- 任意一张 GPU 不达标 → 整机不通过。
- **同批次横向比对**：单机通过但性能低于同批次中位数 10% 以上 → 判 FAIL。
  这一条用于抓"单台看不出、批次里明显掉队"的机器（通常是散热或供电问题）。

### 5.2 验收流程与时间安排

| 阶段 | 内容 | 时长 |
|---|---|---|
| 1 | 硬件级诊断（fieldiag） | 每台约 1～2 h |
| 2 | 单机逐项自动判定（§1～§4、§7） | 每台约 1 h |
| 3 | 长稳烤机（§8，见 C3） | 18 h（或 24 h）|
| 4 | 网络与跨节点 NCCL（§5、§6） | 全集群 4～8 h |
| 5 | 同批次横向比对 | 1 h |
| 6 | 出具验收报告，双方签字 | — |

请贵司在交付时提供：每台机器的物理位置/机柜编号/SN 对照表、带外管理访问方式、
以及一个可插 U 盘或可挂载 ISO 的运维通道。

`【应答】` 贵司是否接受上述验收流程？验收窗口期需要多久？
是否接受"未通过即换机"？换机的时限承诺是多少？

### 5.3 验收标准

单机阈值见**附录 A**，集群级阈值见 2.3.2 与 2.3.3。

---

## 6. 服务与 SLA

| 项目 | 要求 | `【应答】` |
|---|---|---|
| 集群可用率 | 月度 ≥ 99.5% | |
| Goodput | 月度 ≥ 95%（定义见 2.6） | |
| 不达标赔偿 | 说明赔偿方式（服务时长补偿 / 费用减免）与计算公式 | |
| 技术支持响应 | P1（训练中断）≤ 15 分钟响应；P2 ≤ 2 小时 | |
| 支持时间 | 7×24 | |
| 支持方式 | 是否配专属技术对接群与专属工程师 | |
| **分布式训练问题支持** | 是否能协助定位 NCCL hang、慢节点、通信性能退化 | |
| **数据传输问题支持** | 是否能协助定位存储瓶颈、迁移失败 | |
| 是否提供性能调优服务 | 如 NCCL 参数、并行策略、dataloader 优化 | |
| 定期健康报告 | 是否提供月度硬件健康与性能趋势报告 | |

> **重点：** 我方最需要的售后能力不是"重启机器"，而是**能一起定位分布式训练问题**。
> 请说明贵司团队在这方面的实际能力与案例。

---

## 7. 商务条款

### 7.1 报价与计费

请按下表分别报价（**B300 与 H200 两套方案都要报**）：

| 项 | 单位 | B300 方案 | H200 方案 |
|---|---|---|---|
| GPU 节点租金 | 元 / 节点 / 月 | | |
| 折合单卡 | 元 / 卡 / 月 | | |
| 热存储 | 元 / TB / 月 | | |
| 冷存储 | 元 / TB / 月 | | |
| 数据迁移一次性费用 | 元 | | |
| 下载 / 出网流量 | 元 / GB | | |
| 管理平台 / 镜像仓库 | 元 / 月 | | |
| 一次性接入或部署费 | 元 | | |
| **首批 16 节点月总价** | 元 / 月 | | |
| **扩容后 32 节点月总价** | 元 / 月 | | |

| 项目 | `【应答】` |
|---|---|
| 最短租期 | |
| 付款方式与账期 | |
| 是否有竞价/闲时更低价的资源池 | |
| 租期内是否涨价，涨价规则 | |
| 提前退租的违约条款 | |
| 是否可开专票、税率 | |

### 7.2 算力规模与供货能力

| 问题 | `【应答】` |
|---|---|
| **短期（1 周内）**可交付的最大规模，按型号分别列出 | |
| 其中哪两种型号可提供的量最大 | |
| **长期（3～6 个月）**哪种型号更容易做到大规模（千卡级） | |
| 目前在手库存 vs 需新采购的比例 | |
| 首批 16 节点的具体交付节奏（是否分批） | |
| 千卡级部署贵司是否有已上线案例，规模多少 | |

### 7.3 交付形态：裸金属 vs K8s

我方倾向：**裸金属独占 + root 权限 + 我方自建调度器**。

若贵司提供托管 K8s 平台，我方可以接受，但必须同时满足：

1. Pod 内可用 **RDMA 直通**（RDMA device plugin），GPUDirect RDMA 可用；
2. **拓扑感知调度**，多机任务能落在同一 leaf 下且不跨 fabric 域；
3. **Gang scheduling**（要么全部就绪要么不启动），避免部分 pod 起来占着卡空转；
4. 可使用我方自建镜像，容器内可加载我方内核模块相关依赖；
5. 提供裸金属逃生通道——性能有疑问时可申请裸机自行复测。

> 常见的坑：托管 K8s 不给 RDMA 直通或拓扑感知调度做得差，NCCL 退化走 TCP，
> 跨节点带宽掉到十分之一，而平台监控上一切正常。这是我方坚持要裸金属复测通道的原因。

| 问题 | `【应答】` |
|---|---|
| 提供哪种形态（裸金属 / K8s / 两者都有） | |
| 是否给 root | |
| 是否可自行更换驱动/内核 | |
| 是否整机独占（不与其他租户共享节点） | |
| 计算网是否独占（不与其他租户共享 fabric） | |
| 有无自研平台，能否提供试用账号让我方先体验 | |

`【应答】` **请提供试用账号或 demo 环境。** 我方希望在签约前先实际操作一遍
提交任务、挂载存储、查看监控的完整流程。

### 7.4 补贴政策

请**详细**说明，这是我方选型的重要因素：

| 问题 | `【应答】` |
|---|---|
| 贵司资源分布在哪些区域/城市/园区 | |
| 各区域**分别**有哪些算力补贴政策（如算力券/智算券/地方专项） | |
| 补贴形式：直接折价 / 事后返还 / 抵扣券 | |
| 补贴比例或额度上限 | |
| 申领主体资格要求（企业注册地、纳税地、行业、项目备案等） | |
| 申领流程与周期 | |
| 补贴与贵司报价的关系：报价是否已含补贴，还是补贴另算 | |
| 政策有效期与不确定性提示 | |

如我方**不在补贴名单内**：

| 问题 | `【应答】` |
|---|---|
| 是否可通过第三方（有资质的合作方）间接享受 | |
| 具体操作方式与合规依据 | |
| 走第三方的额外成本（服务费/税费/手续费）是多少，怎么收 | |
| 走第三方后合同主体是谁，售后与 SLA 责任如何界定 | |
| 是否有协助申请补贴资格的服务 | |

> 我方要求：**任何补贴安排都必须合规、可书面化、责任主体清晰。**
> 不接受口头承诺或无法写入合同的安排。

### 7.5 扩容方案

| 问题 | `【应答】` |
|---|---|
| 从 128 卡扩到 256 卡的 lead time | |
| 扩容是否需停机，需停多久 | |
| 扩容节点是否与首批**同一 fabric 域、无收敛** | |
| 同域内最大可扩容到多少卡 | |
| 超过该上限后的组网方案与性能影响 | |
| **扩容单价是否锁定**（承诺不高于首批单价） | |
| 是否可预约/预留资源，预留是否收费 | |
| 是否支持临时弹性扩容（按天/周） | |
| 扩容后是否需要重新验收（我方要求：需要） | |

### 7.6 数据安全与退出

| 项目 | 要求 | `【应答】` |
|---|---|---|
| 保密协议 | 需签署 NDA | |
| 数据所有权 | 明确归我方所有 | |
| 数据使用限制 | 贵司不得访问、复制或将我方数据用于任何用途（含模型训练） | |
| 数据加密 | 传输加密、静态加密是否支持 | |
| 访问审计 | 是否有运维人员访问日志，可否提供给我方 | |
| 退租数据处理 | 需提供**数据彻底删除证明** | |
| 数据迁出 | 退租时的迁出带宽与流量费，是否有免费额度 | |
| 迁出窗口期 | 退租后数据保留多久供迁出 | |

---

## 8. 应答要求

1. **应答文件**：正文按本文档章节顺序逐条回复，另填 `vendor_response_template.csv`。
2. **实测数据**：2.3.2、2.3.3、3.2 三节的性能指标必须提供**实测日志原文**，
   不接受标称值。无法提供的请注明"未测"。
3. **样机验证**：我方希望在正式下单前，用贵司一台样机跑一遍附录 A 的单机验收流程，
   以及至少 2 节点的跨节点 NCCL 测试。请说明是否可安排及所需时间。
4. **试用账号**：请提供，见 7.3。
5. **应答截止**：【待填】

### 评估权重（供参考）

| 维度 | 权重 |
|---|---|
| 硬件规格达标度（含 NVLink 8 卡单域、NIC 配比） | 25% |
| 网络与跨节点通信实测性能 | 20% |
| 存储性能（尤其小文件与元数据） | 15% |
| 稳定性与故障恢复（Goodput、真实故障统计） | 15% |
| 价格（含补贴后实际成本） | 15% |
| 售后与技术支持能力 | 10% |

---

## 附录 A：单机验收阈值

以下为我方逐台实测的判定阈值，来源于自建验收工具链的机型档案
（`profiles/b300_8gpu.env` / `profiles/h200_8gpu.env`）。
标 `[标准]` 的为合同判定依据，标 `[推导]` 的注明了推导过程。

### A.1 B300 / GB300

| 章节 | 项目 | 阈值 | 来源 |
|---|---|---|---|
| §1 | GPU 数量 | = 8 | [标准] |
| §1 | 系统内存总量 | ≥ 3 TB | [标准] |
| §1 | 内存识别率（free 实测 / dmidecode 已装） | ≥ 97% | [推导] 抓"内存条未被识别或被 BIOS 屏蔽" |
| §1 | 内存条一致性 | 所有 DIMM 同容量同频率 | [推导] |
| §1 | GPU 温度 | ≤ 86 °C | [标准] |
| §3 | 单卡显存 | ≥ 286000 MiB | [推导] 288 GiB × 97%，抓坏 HBM stack 被屏蔽（−12.5%） |
| §3 | ECC 模式 | Enabled（8/8） | [标准] |
| §3 | ECC 不可纠错 | = 0 | [标准] |
| §3 | ECC 可纠错增量（压测窗口内，单卡） | ≤ 2 | [标准] |
| §3 | 单卡功率上限 | 1100 W ± 10 W | [标准] |
| §3 | PCIe 链路 | Gen6 x16 | [标准] |
| §3 | H2D / D2H 带宽 | ≥ 55 GB/s | [标准] |
| §3 | gpu_burn 短时压测 | 3600 s 无错 | [标准] |
| **§4** | **NVLink 拓扑** | **56/56 GPU 对 = NV18** | **[标准]** |
| **§4** | **每卡 NVLink 链路数** | **18 条，全 Active** | **[标准]** |
| §4 | NVLink CRC / Replay 错误 | = 0 | [标准] |
| §4 | D2D 读带宽（单向） | ≥ 770 GB/s | [标准] 标称 900 的 85.6% |
| §4 | P2P 带宽（单向） | ≥ 750 GB/s | [标准] |
| §4 | P2P 延迟 | ≤ 5 μs | [标准] |
| §4 | 8 卡 NCCL AllReduce Bus BW | ≥ 800 GB/s | [标准] |
| §4 | 8 卡 NCCL AllGather Bus BW | ≥ 650 GB/s | [标准] |
| §7 | 驱动版本 | ≥ 580.105，全集群一致 | [标准] |
| §7 | CUDA | ≥ 13.0 | [标准] |
| §7 | NCCL | ≥ 2.30 | [推导] |
| §7 | DCGM | ≥ 4.0 | [推导] 3.3.x 不识别 Blackwell |
| §7 | nvidia_peermem | 已加载 | [标准] |
| §8 | 长稳烤机时长 | 18 h（见 C3） | [标准] |
| §8 | 烤机期间温度波动 | ≤ 5 °C | [标准] |
| §8 | 烤机期间 XID 错误 | = 0 | [标准] |
| §8 | 烤机期间 NVLink CRC 增量 | = 0 | [标准] |

### A.2 H200

结构同上，主要差异：显存 ≥ 140000 MiB、功率上限 700 W、PCIe Gen5 x16、
D2D ≥ 355 GB/s、P2P ≥ 345 GB/s、8 卡 AllReduce ≥ 350 GB/s、驱动 ≥ 550.54、
CUDA ≥ 12.4、系统内存 ≥ 2 TB。

> H200 的部分阈值标注为 `[待校准]`——甲方原始验收标准是针对 B300 编写的，
> H200 的对应数值由标称规格按同比例推出，**首批实测后回填，或以 OEM 的 ATP
> 报告为准**。若最终采购 H200，双方需就这部分阈值另行书面确认。

### A.3 §2 基础配置规格核对

到货后按采购清单逐台核对：品牌、CPU 型号与路数、内存条数/单条容量/类型/频率、
NVMe 数量与单盘容量、GPU 型号与数量、系统盘配置。
**请在合同附件中提供逐台的 BOM 清单与 SN 对照表。**

---

## 附录 B：需供应商提供的实测日志清单

交付前请准备以下原始日志，验收时逐项核对：

| 编号 | 内容 | 范围 |
|---|---|---|
| B1 | `nvidia-smi topo -m` | 每台 |
| B2 | `nvidia-smi nvlink -s` / `-e` | 每台 |
| B3 | `nvidia-smi -q`（完整） | 每台 |
| B4 | `dmidecode -t memory` | 每台 |
| B5 | `lspci -vvv`（网卡部分，含 LnkSta） | 每台 |
| B6 | `ibstat` / `ethtool` | 每台 |
| B7 | `ib_write_bw` / `ib_write_lat` | 节点对，覆盖同 leaf 与跨 spine |
| B8 | 8 卡 `all_reduce_perf` | 每台 |
| B9 | 2/4/8/16 节点 `all_reduce_perf` | 全集群 |
| B10 | 存储 fio / mdtest / IOR | 全集群 |
| B11 | 近 3 个月同规模集群故障统计 | 参考 2.6 |

---

## 附录 C：术语

| 术语 | 含义 |
|---|---|
| Goodput | 有效训练时间占比，见 2.6 |
| Bus BW | NCCL 测试报告的总线带宽，反映实际可用的集合通信带宽 |
| NV18 | `nvidia-smi topo -m` 中表示两卡之间有 18 条 NVLink 直连 |
| Rail-optimized | 轨道优化组网，节点内第 i 张网卡接入第 i 组 leaf 交换机 |
| SDC | Silent Data Corruption，静默数据损坏 |
| RTO | Recovery Time Objective，故障恢复目标时长 |
| PFS | Parallel File System，并行文件系统 |
| GDR | GPUDirect RDMA |
